# string

* Create string
* access characters
* string operations
  * concat two strings
  * iterating through a string
  * membership test
* string formating
  * escape characters
  * triple quotes ''' 
  * format() method
* Build-in functions
  * enum
* Common string methods:
  * toupper()
  * tolower()
  * find()
  * replace()
  * split()
  * join()